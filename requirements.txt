# Core
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11

# Plotting + tables
matplotlib>=3.8
seaborn>=0.13

# Transformers + LLM
# NOTE: torch is installed separately on Helios via slurm/setup_venv.sh from
# the cu126 wheel index (PyPI ships CPU-only torch for linux_aarch64).
# On a local Mac or CUDA x86_64, plain `pip install torch` works.
transformers>=4.44
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.2.0
tokenizers>=0.19
huggingface_hub>=0.23

# Stats / IAA
statsmodels>=0.14

# Notebooks
jupyter>=1.0
nbconvert>=7.16
ipywidgets>=8.1

# Misc
tqdm>=4.66
pyyaml>=6.0
